import Foundation

enum WidgetVoiceCaptureLink {
    static let dictation = URL(string: "https://wiki.kinic.xyz/ios-voice-capture?mode=dictation")!
    static let voiceMemo = URL(string: "https://wiki.kinic.xyz/ios-voice-capture?mode=voice_memo")!
}
