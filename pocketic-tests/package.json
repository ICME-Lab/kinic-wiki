{
  "name": "llm-wiki-pocketic-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:probe": "cargo build -p ic-sqlite-vfs-probe --target wasm32-unknown-unknown --release",
    "build:vfs-upgrade-fixtures": "bash ../scripts/build-vfs-git-upgrade-fixtures.sh",
    "test": "npm run build:probe && npm run build:vfs-upgrade-fixtures && node scripts/run-tests.mjs"
  },
  "devDependencies": {
    "@dfinity/candid": "3.4.3",
    "@dfinity/pic": "^0.21.0"
  }
}
